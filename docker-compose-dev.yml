services:
  db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_DB: ow
      POSTGRES_USER: ow-dev
      POSTGRES_PASSWORD: dev-only
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
